{
  "name": "detent-demo-capture",
  "private": true,
  "description": "Capture and stitch tooling for the demonstration. Outside the pnpm workspace (which globs packages/* only) on purpose: it pulls a large ffmpeg binary that nothing in the product needs.",
  "devDependencies": {
    "ffmpeg-static": "^5.2.0"
  }
}
